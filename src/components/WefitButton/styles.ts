import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    buttonContainer: {
        flexDirection: "row",
        alignItems: "center",
        fontWeight: "bold",
        padding: 12,
        borderWidth: 1,
        borderRadius: 4,
	},
    buttonTitle: {
        fontWeight: "500", 
        fontSize: 15
    },
    icon: {
        width: 22,
        height: 22,
        marginLeft: 5
    }
      

})

export default styles;